import 'package:flutter/material.dart';

class accountscrren extends StatelessWidget {
  const accountscrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
