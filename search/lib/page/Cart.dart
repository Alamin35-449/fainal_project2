import 'package:flutter/material.dart';

class cartscrren extends StatelessWidget {
  const cartscrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
